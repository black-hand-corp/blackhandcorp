import AuthForm from "../components/AuthForm"; // Adjust the path as needed based on your folder structure
import "/style/style.css"; // The original CSS you uploaded

export default function AuthPage() {
  return <AuthForm />;
}
